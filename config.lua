Config = {}

-- /propcheck command: Displays hash numbers above props in the game
-- /selectprop command: Select the nearest prop and copy its hash
-- Copy the hash to the clipboard and you will get the prop object name here: https://gta-objects.xyz/objects/search?text=1071807406

-- The distance within which to search for props (meters)
Config.SearchDistance = 20.0

-- Duration for which the 3D text above the props is displayed (seconds)
Config.DisplayDuration = 20.0
